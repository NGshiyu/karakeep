interface:
  display_name: "Karakeep"
  short_description: "Repo-specific patterns and workflows for karakeep"
  default_prompt: "Use the karakeep repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true